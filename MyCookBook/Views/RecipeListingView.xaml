<UserControl x:Class="MyCookBook.Views.RecipeListingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:MyCookBook.Views" 
             xmlns:converters="clr-namespace:MyCookBook.Converters"
             mc:Ignorable="d" 
             Name="UserControl"
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
        <converters:PercentageConverter x:Key="PercentageConverter" />
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <TextBlock 
            Grid.Row="0" 
            Grid.Column="1" 
            FontSize="24" 
            MaxWidth="{Binding ActualWidth, ElementName=UserControl, Converter={StaticResource PercentageConverter}, ConverterParameter=0.7}"
            Text="{Binding Name}" />

        <Button 
            Grid.Row="0"
            Grid.Column="0"
            Content="Back" 
            Command="{Binding BackCommand}"
            Padding="5"
            Margin="0 0 20 0"/>

        <Button 
            Grid.Row="0"
            Grid.Column="2"
            Content="Add" 
            Command="{Binding AddCommand}"
            Padding="5"
            Margin="20 0 0 0"/>

        <Grid Margin="0 20 0 0" Grid.Row="1" Grid.Column="1">
            <TextBlock 
                Text="No recipes have been created yet. Click &quot;Add&quot; to add a recipe." 
                Visibility="{Binding HasRecipes, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>

            <ListView 
                ItemsSource="{Binding Recipes}"
                SelectedItem="{Binding SelectedRecipe, Mode=OneWayToSource}"
                Visibility="{Binding HasRecipes, Converter={StaticResource BooleanToVisibilityConverter}}">

                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="SelectionChanged">
                        <i:InvokeCommandAction Command="{Binding SelectRecipeCommand}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel MaxWidth="{Binding ActualWidth, ElementName=UserControl, Converter={StaticResource PercentageConverter}, ConverterParameter=0.75}">
                            <TextBlock 
                                Margin="0 5"
                                FontSize="18"
                                Text="{Binding Name}" />

                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    Margin="0 0 5 0"
                                    Text="Time:" />
                                <TextBlock
                                    Margin="0 0 20 0"
                                    Text="{Binding Minutes}" />

                                <TextBlock
                                    Margin="0 0 5 0"
                                    Text="Servings:" />
                                <TextBlock
                                    Margin="0 0 20 0"
                                    Text="{Binding Servings}" />
                            </StackPanel>

                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
        </Grid>

    </Grid>
</UserControl>
