<UserControl x:Class="MyCookBook.WPF.Views.CreateCategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyCookBook.WPF.Views" 
             xmlns:converters="clr-namespace:MyCookBook.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="350">

    <UserControl.Resources>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="20 10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Visibility="{Binding IsEditing, Converter={StaticResource InverseBooleanToVisibilityConverter}}" FontSize="16" Text="Name" />
            <TextBlock Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}" FontSize="16" Text="New Name" />
        </Grid>

        <TextBox Grid.Row="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="300" Margin="0 10 0 0" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 10 0 0">
            <Button Content="Submit" Command="{Binding SubmitCommand}" IsDefault="True" Padding="5" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" IsCancel="True" Margin="10 0 0 0" Padding="5" />
        </StackPanel>
    </Grid>
</UserControl>
